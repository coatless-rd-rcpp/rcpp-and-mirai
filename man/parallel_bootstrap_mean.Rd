% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/parallel_bootstrap_mean.R
\name{parallel_bootstrap_mean}
\alias{parallel_bootstrap_mean}
\title{Bootstrap the Mean in Parallel with mirai}
\usage{
parallel_bootstrap_mean(x, reps = 10000L, chunks = NULL)
}
\arguments{
\item{x}{A \code{numeric} vector to resample.}

\item{reps}{Total number of bootstrap resamples to draw.}

\item{chunks}{Number of pieces to split \code{reps} into, one task per piece.
Defaults to the number of connected daemons.}
}
\value{
A \code{numeric} vector of length \code{reps} holding the bootstrap distribution of the
mean.
}
\description{
Distributes a bootstrap of the mean across a pool of \code{mirai} daemons. Each
daemon evaluates the packaged C++ kernel \code{\link[=bootstrap_mean]{bootstrap_mean()}} on a chunk of the
replicates, and the pieces are combined into a single bootstrap distribution.
When no daemons are running, the kernel is evaluated once in the current
session instead.
}
\details{
Daemon set up is intentionally left to the caller, in keeping with the design
of \code{mirai}. Start a pool with \code{\link[mirai:daemons]{mirai::daemons()}} before calling this function
to run in parallel, and reset it with \code{mirai::daemons(0)} when finished. The
number of connected daemons is read from \code{\link[mirai:status]{mirai::status()}}.

The work is sent to the daemons through the package worker \code{bootstrap_chunk()}
rather than an inline function. Because that worker lives in the package
namespace, \code{mirai} transmits only a reference to it, not a copy of this
function's environment, and each daemon loads the package (and its compiled
code) to run it. Defining the worker inline would instead capture the whole
calling frame and send it to every daemon.

When daemons are running, each one draws from its own independent random
number stream. Pass a \code{seed} to \code{\link[mirai:daemons]{mirai::daemons()}} to make those streams
reproducible for a fixed number of \code{chunks}. The in-session fallback draws
from the ordinary session stream, so its values will not match the parallel
path.
}
\examples{
x = rnorm(500)

# With no daemon pool the kernel runs in the current session.
boot = parallel_bootstrap_mean(x, reps = 1000)
str(boot)

# Set up daemons to draw the resamples in parallel.
if (interactive()) {
  mirai::daemons(2)
  boot = parallel_bootstrap_mean(x, reps = 1e5)
  mirai::daemons(0)
  quantile(boot, c(0.025, 0.5, 0.975))
}
}
