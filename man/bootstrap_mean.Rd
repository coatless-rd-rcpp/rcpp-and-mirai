% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/RcppExports.R
\name{bootstrap_mean}
\alias{bootstrap_mean}
\title{Bootstrap the Mean of a Numeric Vector}
\usage{
bootstrap_mean(x, reps)
}
\arguments{
\item{x}{A \code{numeric} vector to resample.}

\item{reps}{The number of bootstrap resamples to draw.}
}
\value{
A \code{numeric} vector of length \code{reps} holding the bootstrap distribution of
the mean.
}
\description{
Draws \code{reps} bootstrap resamples (sampling with replacement) of \code{x} and
returns the mean of each resample. This is the compute kernel that the
package distributes across \emph{mirai} daemons.
}
\details{
The routine uses \emph{R}'s random number generator through \code{R::unif_rand()}.
When the kernel runs on a \emph{mirai} daemon, each daemon is given its own
independent random number stream, so resamples drawn in parallel remain
statistically sound.
}
\examples{
bootstrap_mean(rnorm(100), reps = 10)

}
